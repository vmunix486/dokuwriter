# Todo

 * Add support for dark mode (default text color and default background color, keeping colored text the same)
 * Change the default file affix to something like `*.dwt`
 * Add support for doing other markdowns, such as Github
 * Make adding rich text more convenient (don't put the markdown stuff in front of the cursor, and only behind it)
 * Add support for italicizing links
 * Add support for code blocks (no highlighting though)
 * Fix the text automatically scrolling to the cursor
 * Fix hiding the rich text markers entirely not working
 * Add support for enabling the color extention in dokuwiki (colored text, should be pretty easy bc coloring text is easy)
 * Add support for loading images (far future)
